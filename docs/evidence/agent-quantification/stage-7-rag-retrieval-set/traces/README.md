# Traces

Representative trace artifacts for this suite are currently the case-level `hits` and `answerPreview` fields in `reports/stage-7-rag-retrieval-set-report.json`.

Add separate trace files here only if a future run captures external API output, UI screenshots, or full debug snapshots that should not be embedded in the summary.
